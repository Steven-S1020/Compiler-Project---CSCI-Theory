total = 0;
fib = 5;

while fib > 0 do
   total = total + fib;
   fib = fib - 1;
endwhile;

if temp > 212
   then boil = 1;
   else if temp <= 32
      then ice = 1;
      else water = 1;
   endif;
endif;
